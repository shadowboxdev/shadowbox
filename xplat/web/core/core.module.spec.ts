describe("Core module generic test", () => {
  it("Should be true", () => {
    expect(true).toBeTruthy();
  });
});

/*
import { ShadowboxCoreModule } from './core.module';

describe('ShadowboxCoreModule', () => {
  it('should work', () => {
    expect(new ShadowboxCoreModule(null)).toBeDefined();
  });
});
*/
