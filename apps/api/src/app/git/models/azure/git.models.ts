import { IGitApi } from 'azure-devops-node-api/GitApi';
import {
  GitRepository,
  GitItem,
} from 'azure-devops-node-api/interfaces/GitInterfaces';

type GitRepositories = GitRepository[];

/** LEAVE AT BOTTOM */
export { IGitApi, GitRepository, GitRepositories, GitItem };
